<h1><%= @cuisine.name %></h1>


<%= @cuisine.recipes.each do |cr|  %>
  
  <h1><%= cr.title %></h1>
  <ul>
    <li><%= cr.recipe_type.name %></li>
    <li><%= cr.cuisine.name %></li>
    <li><%= cr.difficulty %></li>
    <li><%= cr.cook_time%> minutos</li>
  </ul>

<%end%>
<% if @cuisine.recipes.empty? %>
    Nenhuma receita encontrada para este tipo de cozinha
<%end%>  